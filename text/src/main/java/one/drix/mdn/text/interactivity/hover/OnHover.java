package one.drix.mdn.text.interactivity.hover;

public interface OnHover {
    HoverEvent showText(final String value);
    // HoverEvent showItem(final String id, final int count, final String components);
    // HoverEvent showEntity(final String name, final String id, final UUID uuid);
}
